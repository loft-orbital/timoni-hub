$schema: 'https://moonrepo.dev/schemas/project.json'

language: cue
type: application
platform: system

project:
  name: '{{name}}'
  description: 'Timoni module for {{name}}'

dependsOn:
  - buildsystem
  - timoni-schemas
  - k8s
  - utils

tags:
  - timoni
